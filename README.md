# React Portfolio Export

## Run locally
1. npm install
2. npm run dev

## Build
1. npm run build
2. npm run preview

## Notes
- This project preserves the same exported template markup, user data, theme colors, and export interactions.
- Resume assets are copied into `public/resume/` when available.
- Source data snapshot is available in `src/portfolio-data.json`.
